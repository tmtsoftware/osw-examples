csw-logging {
  component-log-levels {
    m1cs {
      M1CSAssemblyClient = debug
    }
  }

  appender-config {
    stdout {
      summary = true
      color = false
      pretty = false
      oneLine = true
    }
  }

  logLevel = info
}
